spring:
  profiles:
    active: dev

  main:
    web-application-type: servlet

  config:
    import: "optional:configserver:"

  cloud:
    discovery:
      enabled: false

eureka:
  client:
    enabled: false

management:
  metrics:
    export:
      otlp:
        url: http://otel-collector:4318/v1/metrics